export const getters = {
    activePost(state) {
        return state.activePost
    },
    activeTag(state) {
        return state.activeTag
    },
    activeTopic(state) {
        return state.activeTopic
    }
}

export default {
    getters,
}
